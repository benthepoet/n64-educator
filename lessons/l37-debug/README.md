# L37 — When it crashes

Debug logging, asserts, and a deliberate crash so you can read the exception screen.

```bash
source scripts/env.sh
make -C lessons/l37-debug
```

Docs: `docs/guide/m0/l37-debug.md` · Reference: `docs/reference/debugging.md`
