# L01 — Hello ROM

First buildable lesson. Canonical write-up: **docs site → Module 0 → L01**.

```bash
make -C lessons/l01-hello-rom
# → lessons/l01-hello-rom/l01_hello.z64
```

Requires `N64_INST` (see setup guide).
